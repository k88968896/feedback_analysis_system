import Layout from "../components/Layout";

const Home = () => {
    return (
        <Layout>
            <h2>歡迎使用管理系統</h2>
            <p>請選擇左側選單進行操作。</p>
        </Layout>
    );
};

export default Home;
